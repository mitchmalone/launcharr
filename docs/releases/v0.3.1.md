## Breaking changes

- none

## Highlights

**Signed & notarized.** Identical to v0.3.0 in features; this build is Developer ID signed
and notarized, so the dmg and zip open clean from a browser — no Gatekeeper detour.
(v0.3.0 shipped unsigned while Apple's first-submission review dragged; brew users were
never affected.)

## Performance receipts

| Metric                         | Budget   | This release |
| ------------------------------ | -------- | ------------ |
| Hotkey → panel visible         | < 100 ms | 4.2 ms       |
| Cold start → hotkey registered | < 1 s    | 183 ms       |
| Idle memory (panel hidden)     | < 120 MB | 98 MB        |

## Install

```sh
brew install mitchmalone/launcharr/launcharr
```

Or grab the dmg / zip below, or build from source (README).

## All changes

```
signing-only release — see v0.3.0 for the feature changelog
```
